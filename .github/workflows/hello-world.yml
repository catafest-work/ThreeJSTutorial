name: 'Hello World!'
description: 'Saying hello to the world!'
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: catafest-work/ThreeJSTutorial@master

